apiVersion: v1
kind: Service
metadata:
  name: promptionary-frontend
  labels:
    app: promptionary
    component: frontend
spec:
  type: LoadBalancer
  selector:
    app: promptionary
    component: frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http